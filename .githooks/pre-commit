#!/bin/sh
# Blocks a commit whose recipe data does not line up. See tools/check-recipes.py.
# Skip it for one commit with: git commit --no-verify

if ! python3 tools/check-recipes.py; then
    echo
    echo "Commit stopped: fix the problems above, or commit with --no-verify."
    exit 1
fi
